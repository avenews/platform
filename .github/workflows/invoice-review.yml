name: Invoice portal review checks

on:
  pull_request:
    branches: [stefan]

permissions:
  contents: read
  statuses: read

concurrency:
  group: invoice-review-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  invoice-review:
    if: github.head_ref == 'stefan-invoice-partner-review'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install application and audit dependencies
        run: |
          npm install --legacy-peer-deps --no-audit --no-fund
          npm install --no-save --legacy-peer-deps --no-audit --no-fund @playwright/test@1.55.0 esbuild@0.25.9
      - name: Baseline, typecheck and production build
        run: |
          mkdir -p review-evidence
          set -o pipefail
          npm run check 2>&1 | tee review-evidence/build.log
      - name: Invoice review domain checks
        run: |
          set -o pipefail
          node scripts/test-invoice-review.mjs 2>&1 | tee review-evidence/domain.log
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Responsive interaction checks
        run: |
          set -o pipefail
          npx playwright test --config invoice-review.playwright.config.mjs 2>&1 | tee review-evidence/interactions.log
      - name: Resolve exact-head Netlify Deploy Preview
        id: preview
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REVIEW_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          REVIEW_PR_NUMBER: ${{ github.event.pull_request.number }}
        run: node scripts/wait-invoice-review-preview.mjs
      - name: Verify deployed desktop, tablet and mobile journeys
        env:
          REVIEW_BASE_URL: ${{ steps.preview.outputs.url }}
        run: |
          set -o pipefail
          npx playwright test --config invoice-review.playwright.config.mjs 2>&1 | tee review-evidence/deployed-interactions.log
      - name: Preserve review evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: invoice-review-evidence-${{ github.event.pull_request.head.sha }}
          path: |
            review-evidence
            review-test-results
            review-playwright-report
          if-no-files-found: warn
          retention-days: 14
