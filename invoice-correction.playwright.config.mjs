import {defineConfig} from '@playwright/test'
const deployed=!!process.env.REVIEW_BASE_URL
export default defineConfig({testDir:'./tests/invoice-correction',fullyParallel:true,workers:2,timeout:60000,expect:{timeout:10000},outputDir:`correction-results/${deployed?'deployed':'local'}`,reporter:[['line'],['json',{outputFile:`correction-evidence/${deployed?'deployed':'local'}.json`}]],use:{baseURL:process.env.REVIEW_BASE_URL||'http://127.0.0.1:4200',colorScheme:'light',screenshot:'only-on-failure',trace:'retain-on-failure',permissions:['clipboard-read','clipboard-write']},webServer:deployed?undefined:{command:'npm start -- --host 127.0.0.1 --port 4200',url:'http://127.0.0.1:4200/login',reuseExistingServer:!process.env.CI,timeout:120000},projects:[{name:'desktop',use:{viewport:{width:1440,height:900}}},{name:'tablet',use:{viewport:{width:768,height:1024}}},{name:'mobile',use:{viewport:{width:390,height:844}}},{name:'minimum-mobile',use:{viewport:{width:320,height:720}}}]})
